# С прокси Vite (npm run dev) — запросы идут на тот же origin, проксируются на localhost:5032
VITE_API_BASE_URL=/api/v1

# Если фронт открыт с другого origin без прокси — укажите полный URL API:
# VITE_API_BASE_URL=http://localhost:5032/api/v1
